<%
let keys = ['reel1', 'reel2', 'reel3', 'reel4','reel5']

let result = {};
for (let key of keys) {
    result[key] = [];
}

for (let obj of data) {
    for (let key of keys) {
        if (obj[key] > 0) {
            result[key].push(obj[key]);
        }
    }
}

for (let key in result) {
    result[key].reverse();
}

%>
<%- JSON.stringify(result, null, 2); %>